.navbar.navbar-default
  .container
    .container-fluid
      %ul.nav.navbar-nav
        %li= link_to(t('.home'), root_path)
        %li= link_to(t('.tag_cloud'), tag_cloud_messages_path)
        - if user_signed_in?
          %li= link_to(t('.new_message'), new_message_path)
          %li= link_to(t('.drafts'), draft_messages_path)
          %li= link_to(t('.profile'), edit_user_registration_path)
          %li= link_to(t('.logout'), destroy_user_session_path)
        - else
          %li= link_to(t('.login'), new_user_session_path)

